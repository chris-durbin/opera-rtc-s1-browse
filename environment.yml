name: opera-rtc-s1-browse
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  # For running
  - asf_search
  - boto3
  - gdal
  # For packaging, and testing
  - pytest
  - pytest-cov
