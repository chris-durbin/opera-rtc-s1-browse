name: opera-rtc-s1-browse
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  # For running
  - boto3
  - earthaccess
  - gdal
  # For packaging, and testing
  - pytest
  - pytest-cov
